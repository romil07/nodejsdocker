on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n danademoacr -g danademoacr978b-rg --location "East US 2" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n danademoacr --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n danademoacr --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n danademoacr --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: danademoacr.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/Application" -f  "Application/Dockerfile" -t danademoacr.azurecr.io/danademocluster:${{ github.sha }}
        docker push danademoacr.azurecr.io/danademocluster:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_danademocluster_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep danademoclusterad67 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace danademoclusterad67`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: danademoclusterad67
        container-registry-url: danademoacr.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: danademoclusdockerauth
       
    - uses: azure/k8s-deploy@v1
      with:
        namespace: danademoclusterad67
        manifests: |
          manifests/deployment-1.yml
          manifests/service-1.yml
        images: |
          danademoacr.azurecr.io/danademocluster:${{ github.sha }}
        imagepullsecrets: |
          danademoclusdockerauth